import React from "react";
import { SafeAreaView, Text } from "react-native";

const Partner = ({ navigation }) => {
    return (
        <SafeAreaView>
            <Text>Partner page</Text>
        </SafeAreaView>
    )
}

export default Partner;